import React from "react";
import { createRoot } from "react-dom/client";
import LuxuryPeps from "./LuxuryPeps.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LuxuryPeps />
  </React.StrictMode>
);
